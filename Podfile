# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CICDProject' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CICDProject

pod 'SDWebImage', '~> 5.0'
  target 'CICDProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CICDProjectUITests' do
    # Pods for testing
  end

end
